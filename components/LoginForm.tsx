"use client";
import {FormEvent,useState} from "react";
import BrandLockup from "@/components/BrandLockup";
export default function LoginForm({returnTo}:{returnTo:string}){const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({identity:f.get("identity"),password:f.get("password")})});const b=await r.json();if(r.ok)location.href=returnTo.startsWith("/")?returnTo:"/admin";else{setError(b.error);setBusy(false)}}return <main className="login-page"><section><BrandLockup href="/"/><span className="eyebrow">Akses internal</span><h1>Masuk sebagai petugas</h1><p>Gunakan username atau email akun yang dibuat oleh Super Admin.</p><form onSubmit={submit}><label>Username / email<input name="identity" required autoFocus autoComplete="username"/></label><label>Password<input type="password" name="password" required autoComplete="current-password"/></label>{error&&<p className="form-error">{error}</p>}<button disabled={busy} className="button primary">{busy?"Memeriksa...":"Masuk"}</button></form></section></main>}
